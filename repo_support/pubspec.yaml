name: cv_repo_support
version: 0.1.0
publish_to: none
deploy_to: none

environment:
  sdk: ^3.9.0

dependencies:
  process_run: '>=0.10.0'

dev_dependencies:
  path:
  test: ">=1.26.3"
  dev_build: '>=1.1.2+11'
  lints: '>=6.0.0'
dependency_overrides:
  cv:
    path: ../packages/cv
